{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run Main with database configuration",
            "request": "launch",
            "mainClass": "Main",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
